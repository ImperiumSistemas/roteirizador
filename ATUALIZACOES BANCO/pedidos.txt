create table pedidos(
	id int not null auto_increment primary key,
    codPedido int, 
    codCliente varchar(45),
    peso varchar(10),
    cubagem varchar(15),
    valorPedido float, 
    codFilial int, 
    cargaErp varchar(50),
    codPraca int, 
    dataPedido date, 
    podeFormarCarga varchar(1),
    statusPedido varchar(15)
);

alter table pedidos 
add foreign key(codCliente) REFERENCES Clientes(codCliente);

alter table pedidos
add foreign key(codPraca) references pracas(codPraca);

alter table pedidos
add foreign key(codFilial) references filial(codFilial);


ALTER TABLE `roteirizador`.`pedidos` 
ADD COLUMN `sequenciaEntrega` INT NULL AFTER `statusPedido`,
ADD COLUMN `cargas_id` INT(11) NULL AFTER `sequenciaEntrega`;

ALTER TABLE `roteirizador`.`pedidos` 
ADD COLUMN `updated_at` VARCHAR(45) NULL AFTER `cargas_id`,
ADD COLUMN `created_at` VARCHAR(45) NULL AFTER `updated_at`;
